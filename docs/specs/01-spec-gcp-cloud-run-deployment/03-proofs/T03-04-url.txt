Type: url
Command: Smoke-check URL returns 200 and is surfaced in run summary
Expected: 200 in GITHUB_STEP_SUMMARY
Timestamp: 2026-04-21T00:00:00Z
Capture: manual
Status: SKIPPED

Rationale:
This proof requires a live deployed Cloud Run service (produced by T03-01). The workflow
includes a post-deploy smoke check step that:
  1. Polls <service-url>/.well-known/oauth-authorization-server with a 60-second deadline
  2. Fails the job if the probe does not return HTTP 200
  3. Appends a summary table to GITHUB_STEP_SUMMARY on success, including the service URL
     and "200 OK" status

The smoke check logic is in the "Post-deploy smoke check" step of gcp-deploy.yml.
