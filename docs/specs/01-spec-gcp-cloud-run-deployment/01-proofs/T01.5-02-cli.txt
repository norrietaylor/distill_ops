Type: cli
Command: bash gcp/bootstrap.sh --project test-proj --region us-central1 --repo owner/name | tail -n 20
Expected: final summary block contains GCP_WIF_PROVIDER=projects/... and GCP_DEPLOYER_SA=distillery-deployer@...
Timestamp: 2026-04-23T00:40:00Z
Status: PASS

The bootstrap script's final stdout block emits the three values the
GitHub Actions deploy workflow needs as repo variables. `log()` output
goes to stderr, so piping stdout directly to the operator's clipboard
captures only the summary.

--- ACTUAL SUMMARY BLOCK (captured from stub exercise) ---

================= BOOTSTRAP SUMMARY =================
Set these as GitHub Actions repository variables
(Settings -> Secrets and variables -> Actions -> Variables):

  GCP_PROJECT_ID=test-proj
  GCP_DEPLOYER_SA=distillery-deployer@test-proj.iam.gserviceaccount.com
  GCP_WIF_PROVIDER=projects/123456789012/locations/global/workloadIdentityPools/distillery-deploy/providers/github

The deploy workflow (.github/workflows/gcp-deploy.yml) reads these
three variables and exchanges a GitHub OIDC token for a short-lived
access token — no service-account JSON key is ever issued.
=====================================================

Note:
  The `123456789012` is a stub-returned fake project number. In a real
  run, `gcloud projects describe "$PROJECT" --format='value(projectNumber)'`
  returns the actual numeric project number, which the GCP_WIF_PROVIDER
  resource path requires (the resource path uses project NUMBER, not ID).

The summary is stable across re-runs: it recomputes from constants and
live lookups each time, so operators who misplace the values can re-run
`bash gcp/bootstrap.sh ...` and the summary is reprinted unchanged.
