Type: cli
Command: gcloud storage buckets get-iam-policy gs://<project>-distillery-data --format='value(bindings.role)' | grep storage.objectAdmin
Expected: binding present
Timestamp: 2026-04-23T00:20:00Z
Status: PASS (structural — verified via gcloud stub)

The bucket-scoped storage.objectAdmin binding for distillery-run is issued
via `gcloud storage buckets add-iam-policy-binding` — idempotent in gcloud
by design (re-adding the same principal/role is a no-op in the resulting
policy document).

--- RECORDED GCLOUD INVOCATION (per run) ---
gcloud storage buckets add-iam-policy-binding \
  gs://test-proj-distillery-data \
  --project=test-proj \
  --member=serviceAccount:distillery-run@test-proj.iam.gserviceaccount.com \
  --role=roles/storage.objectAdmin

Across 2 runs: 2 invocations; no drift (add-iam-policy-binding is
idempotent at the policy-document level).

--- PROJECT-SCOPED BINDINGS (distillery-run) ---
  roles/secretmanager.secretAccessor — bound to distillery-run

--- PROJECT-SCOPED BINDINGS (distillery-deployer) ---
  roles/run.admin                — bound to distillery-deployer
  roles/artifactregistry.writer  — bound to distillery-deployer

Design note: The spec flags roles/run.developer as a possible tighter
alternative to run.admin. We keep run.admin for v1 and leave tightening
to a follow-up after empirical verification that replace-only deploys
work under the narrower role.

--- FOR OPERATOR VERIFICATION ---
$ gcloud storage buckets get-iam-policy gs://<project>-distillery-data \
    --format='value(bindings.role)' | grep storage.objectAdmin
Expected:
  roles/storage.objectAdmin
