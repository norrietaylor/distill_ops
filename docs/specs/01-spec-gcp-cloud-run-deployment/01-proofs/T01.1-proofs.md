# T01.1 Proof Summary — Bootstrap script skeleton + API enablement

**Task:** T01.1 — Bootstrap script skeleton + API enablement
**Spec:** `docs/specs/01-spec-gcp-cloud-run-deployment/01-spec-gcp-cloud-run-deployment.md`
**File delivered:** `gcp/bootstrap.sh`

## Requirements coverage

| ID    | Requirement                                                                  | Status |
|-------|------------------------------------------------------------------------------|--------|
| R01.1 | Accepts --project, --region, --repo as flags or env vars                     | PASS   |
| R01.2 | Enables run/artifactregistry/iamcredentials/storage/secretmanager APIs       | PASS (code path verified; runtime proof requires gcloud) |
| R01.h | `bash gcp/bootstrap.sh --help` prints usage and exits 0                      | PASS   |

## Proof artifacts

| #  | Type | Command                              | Expected                      | Result                        |
|----|------|--------------------------------------|-------------------------------|-------------------------------|
| 01 | cli  | `bash gcp/bootstrap.sh --help`       | prints usage, exits 0         | PASS — see T01.1-01-cli.txt   |
| 02 | cli  | `shellcheck gcp/bootstrap.sh`        | no errors                     | SKIPPED (shellcheck unavailable); `bash -n` substituted — see T01.1-02-cli.txt |
| 03 | file | `test -x gcp/bootstrap.sh`           | executable bit set            | PASS — see T01.1-03-file.txt  |

## Scope boundary (intentional carry-over)

This sub-task intentionally scaffolds only arg parsing, validation, and
API enablement. The following calls are placeholder-free and will be
appended to `main()` by subsequent sub-tasks:

- T01.2 — GCS data bucket (`create_bucket`)
- T01.3 — Artifact Registry repo (`create_ar_repo`)
- T01.4 — Service accounts + IAM bindings (`create_service_accounts`, `bind_iam`)
- T01.5 — Workload Identity Federation pool/provider + final summary block

Each will extend `gcp/bootstrap.sh` additively. No TODO/FIXME placeholders
are present; the current `main()` is a valid terminating path on its own.

## Environment notes

- `gcloud` not installed in this worktree — the script's `require_gcloud`
  guard exits with a clear error; deploy/CI environments will satisfy it.
- `shellcheck` not installed — `bash -n` used for syntax validation.
- All negative-path argument validations exercised and correct (see
  T01.1-02-cli.txt for the full matrix).
