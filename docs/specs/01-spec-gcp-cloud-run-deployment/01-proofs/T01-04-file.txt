Type: file
Command: test -x gcp/bootstrap.sh
Expected: file exists and is executable
Timestamp: 2026-04-21T14:49:42Z
Status: PASS

--- FILESYSTEM METADATA ---

$ ls -la gcp/bootstrap.sh
-rwxrwxr-x 1 norrie norrie 15463 Apr 23 07:44 gcp/bootstrap.sh

$ test -x gcp/bootstrap.sh && echo "exists and is executable"
exists and is executable

--- SYNTAX CHECK (bash -n) ---

$ bash -n gcp/bootstrap.sh
(no output; exit 0)

--- USAGE / --help SMOKE ---

$ bash gcp/bootstrap.sh --help
Usage: bash gcp/bootstrap.sh --project <id> --region <region> --repo <owner/name>
...
(full usage text printed; exit 0)

--- SHEBANG + set -euo pipefail ---

$ head -n 3 gcp/bootstrap.sh
#!/usr/bin/env bash
# gcp/bootstrap.sh -- idempotent provisioner for the Distillery Cloud Run deployment.
#

$ grep -n '^set -euo pipefail' gcp/bootstrap.sh
19:set -euo pipefail

Both scope patterns enforced:
  - `set -euo pipefail` present (line 19)
  - Accepts config via --project/--region/--repo flags AND GCP_PROJECT/
    GCP_REGION/GCP_REPO env vars, with `--help` output
  - Uses `gcloud describe` -> conditional-create everywhere (no blanket `|| true`)
  - No hardcoded project IDs or user emails (all derived from --project/--repo flags)
